{
  "name": "dsh-model-caps-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Source repository for the dsh-model-caps plugin. The installable package is built into package/.",
  "type": "module",
  "scripts": {
    "build": "node build-model-caps.mjs",
    "test": "node run-tests.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
