# Build output: regenerated by `node build-model-caps.mjs`.
package/
node_modules/

# Local scratch: the suites write inside the checkout (`.tmp/`), never to the
# system temp directory, so this is the only place they touch.
.tmp/
*.log
*.tmp
