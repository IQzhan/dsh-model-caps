/**
 * dsh-model-caps client.
 *
 * No settings page. Saving a custom provider already notifies the Host,
 * which fills blank caps. This module stays so the package still has a
 * web client entry and does not register a section.
 *
 * @module dsh-model-caps/client
 */

export const inject = []

export function apply() {}
